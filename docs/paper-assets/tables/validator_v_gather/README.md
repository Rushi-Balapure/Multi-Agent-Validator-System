Live gather `run_id` `validator-v-gather-development-s0-n20-90129d9056fd` from Turing-Machine `artifacts/validator_v/{predictions.jsonl,run.json}`.

Claim ids match B2 live `same-evidence-b2-development-s0-n20-dac855c4e2ae`. Every prediction row has `execution_status=failed` (fail-closed D0: gather does not load gold D0 citations). Scorer report: `v_gather_metrics.json` with `scoring_status=skipped_not_ok`, `n_skipped_not_ok=20`, `n_scored=0`, empty FE metrics (N/A — not 0.0).
